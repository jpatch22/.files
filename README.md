## nvim
# Install Packages
Add to packer file, :so, :PackerSync

## MAC
yabai --start-service
skhd --start-service

## Linking to correct places
Mac: ln -s ~/.files/file ~/file
nvim -> .config/nvim 
skhd -> .config/skhd
yabai -> .config/yabai

